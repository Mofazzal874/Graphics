# 3D Hover Vehicle Game

See the [project overview](../README.md) for screenshots, architecture, controls, and build instructions.

Open `HoverVehicleGame.sln` in Visual Studio 2022, select **x64**, and build. Dependencies are included in `vendor/`.
